def area_triangulo(base, altura):
    return (base * altura) / 2

# Ejemplo de uso:
base = 5
altura = 8
print(f"El área del triángulo con base {base} y altura {altura} es: {area_triangulo(base , altura)}")

def area_triangulo(case, altura):
    return (base * altura) / 2
# Ejemplo de uso:
base = 5
altura = 8

print(f"El área del triángulo con base {base} y altura{altura} es: {area_triangulo(base, altura)}")